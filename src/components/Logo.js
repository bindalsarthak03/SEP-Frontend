import { Heading } from '@chakra-ui/react';
import React from 'react';
// import { Link } from 'react-router-dom';

const Logo = props => {
    return (

        <Heading {...props}>Codestream</Heading>

    );
};

export default Logo;